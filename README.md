# EMI_calc

Static EMI calculator rebuilt in Astro.js with production SEO metadata.

## Commands

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

## Notes

This project uses Astro to keep a lightweight, static HTML build while preserving canonical, Open Graph, Twitter, and schema markup for the EMI calculator page.
